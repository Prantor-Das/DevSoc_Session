{
  "buildCommand": "cd client && npm install --production=false && npm run build && cd ../server && npm install --production",
  "outputDirectory": "client/dist",
  "installCommand": "echo 'Skip root install'",
  "rewrites": [
    {
      "source": "/api/v1/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
